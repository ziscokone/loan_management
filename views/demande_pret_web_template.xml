<?xml version="1.0" encoding="utf-8"?>
<odoo>

<!-- Erreur de matricule : Matricule inexistant dans la BD  -->
<template id="matricule_error" name="Erreur Matricule">
    <t t-call="website.layout">
        <style>
            .background-container {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
                            url('/loan_management/static/src/img/paa.jpg') center/cover no-repeat;
                filter: blur(4px);
                z-index: -1;
            }

            .error-card {
                background: rgba(255, 255, 255, 0.95);
                backdrop-filter: blur(5px);
            }
        </style>

        <!-- Background image container -->
        <div class="background-container"></div>

        <!-- Main content -->
        <div class="d-flex align-items-center justify-content-center min-vh-100">
            <div class="card shadow-lg border-0 rounded-lg p-5 text-center error-card" style="max-width: 500px;">
                <h5 class="card-title text-danger fw-bold mb-4" style="font-size: 1.5rem;">
                    <t t-esc="error"/>
                </h5>
                <div class="mt-4">
                    <a href="/matricule" class="btn btn-success px-4 py-2">
                        Réessayer
                    </a>
                </div>
            </div>
        </div>
    </t>
</template>

    <!-- Template  de vérification du matricule -->
<template id="matricule_employee_verification" name="Vérification Employé">
    <t t-name="website.matricule_employee_verification">
        <t t-call="website.layout">
            <div class="o_portal_wrap min-vh-100 d-flex align-items-center position-relative">
                <!-- Background image avec flou -->
                <div class="position-absolute w-100 h-100" 
                     style="top: 0; 
                            left: 0; 
                            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), 
                                      url('/loan_management/static/src/img/paa.jpg') center/cover no-repeat;
                            filter: blur(2px);
                            z-index: -1;">
                </div>
                
                <div class="container o_website_form_container position-relative">
                    <div class="row justify-content-center">
                        <div class="col-12 col-sm-8 col-md-6 col-lg-4">
                            <div class="card shadow border-0 rounded-3">
                                <div class="card-body p-3">
                                    <div class="text-center mb-3">
                                        <h6 class="text-muted">Entrez votre matricule pour continuer</h6>
                                    </div>
                                    <form t-attf-action="/demande-pret" method="post" class="o_website_form">
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        
                                        <div class="mb-3">
                                            <input type="text" 
                                                   name="matricule" 
                                                   required="1"
                                                   class="form-control"
                                                   placeholder="Entrez votre matricule"/>
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100" style="background-color:#0099CC">
                                            Continuer
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </t>
</template>

    <!-- Template du Formulaire de demande  -->
   <template id="loan_application_form" name="Formulaire de Demande de Prêt">
       <t t-call="website.layout">
           <div class="min-vh-100 o_loan_form_wrapper">
                <div class="position-absolute w-100 h-100" 
                     style="top: 0; 
                            left: 0; 
                            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), 
                                      url('/loan_management/static/src/img/paa_2.jpg') center/cover no-repeat;
                            filter: blur(2px);
                            z-index: -1;">
                </div>
                
               <div class="container py-5">
                   <div class="row justify-content-center">
                       <!-- Section Formulaire centrée -->
                       <div class="col-lg-8">
                           <div class="card shadow-sm border-0 rounded-3">
                               <div class="card-header text-white py-3 border-bottom-0" style="background-color:#0099CC">
                                   <h4 class="card-title text-center mb-0">
                                       Fiche de demande de prêt scolaire
                                   </h4>
                               </div>
                               <div class="card-body p-4">
                                   <form id="loanForm" t-attf-action="/demande-pret/preview" method="post" class="needs-validation" novalidate="1">
                                       <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                       <!-- Campagne et Matricule -->
                                       <div class="row mb-4">
                                           <div class="col-md-6">
                                               <label class="form-label">
                                                   <i class="fa fa-calendar me-2"/>Campagne
                                               </label>
                                               <input type="text" class="form-control" 
                                                      t-att-value="campaign.name" 
                                                      readonly="1"/>
                                               <input type="hidden" name="campaign_id" 
                                                      t-att-value="campaign.id"/>
                                           </div>
                                           <div class="col-md-6">
                                               <label class="form-label">
                                                   <i class="fa fa-id-card me-2"/>Matricule 
                                               </label>
                                               <div class="input-group">
                                                   <input type="text" t-att-value="employee.matricule" name="matricule" class="form-control" readonly="1"  placeholder="Votre matricule"/>
                                               </div>
                                               <div id="matricule-message" class="mt-2" style="display: none;">
                                               </div>
                                           </div>
                                       </div>

                                       <!-- Informations personnelles -->
                                       <div class="row mb-4">
                                           <div class="col-md-6">
                                               <label class="form-label">
                                                   <i class="fa fa-user me-2"/>Nom 
                                               </label>
                                               <input type="text" id="nom" t-att-value="employee.name" name="nom" class="form-control" readonly="1"/>
                                           </div>
                                           <div class="col-md-6">
                                               <label class="form-label">
                                                   <i class="fa fa-user me-2"/>Prénoms 
                                               </label>
                                               <input type="text" id="prenoms" t-att-value="employee.prenoms" name="prenoms" class="form-control" readonly="1"/>
                                           </div>
                                       </div>

                                       <!-- Informations complémentaires -->
                                       <div class="row mb-4">
                                           <div class="col-md-4">
                                               <label class="form-label">
                                                   <i class="fa fa-venus-mars me-2"/>Sexe
                                               </label>
                                               <input type="text" id="sexe" name="sexe" t-att-value="employee.sexe" readonly="1" class="form-control"/>
                                           </div>

                                           <!-- Champs AGE  caché : pas ne necessaire comme info sur le formulaire visuel  -->
                                           <div class="col-md-4">
                                               <label class="form-label">
                                                   <i class="fa fa-sitemap me-2"/>Direction
                                               </label>
                                                <input type="text" id="direction" t-att-value="employee.nom_direction" readonly="1"  name="direction" class="form-control"/>
                                                <input type="hidden" id="age" name="age" t-att-value="employee.age" readonly="1" class="form-control"/>
                                                <input type="hidden" id="anciennete" name="anciennete" t-att-value="employee.anciennete" readonly="1" class="form-control"/>
                                                <input type="hidden" name="employee_id" t-att-value="employee.id"/>
                                           </div>

                                           <div class="col-md-4">
                                               <label class="form-label">
                                                   <i class="fa fa-building me-2"/>Catégorie
                                               </label>
                                               <input type="text" id="typecategorie" t-att-value="employee.typecat" readonly="1" name="typecategorie" class="form-control"/>
                                           </div>
                                       </div>


                                        <!-- Informations personnelles  : EMAIL + TELEPHONE-->
                                        <div class="row mb-4">
                                            <div class="col-md-6">
                                                <label class="form-label">
                                                    <i class="fa fa-phone me-2"></i>Téléphone 
                                                </label>
                                                <input type="number" id="telephone" 
                                                    name="telephone" class="form-control" 
                                                    maxlength="10" placeholder="Ex: 0701020304" required="required"
                                                    t-att-value="request.params.get('telephone')"
                                                    oninput="this.value = this.value.replace(/[^0-9]/g, ''); if (this.value.length > this.maxLength) this.value = this.value.slice(0, this.maxLength); if (parseInt(this.value) &lt; 0) this.value = '';"
                                                />
                                            </div>
                                            <div class="col-md-6">
                                                <label class="form-label">
                                                    <i class="fa fa-envelope me-2"></i>Email 
                                                </label>
                                                <input type="email" id="email" 
                                                    name="email" class="form-control" 
                                                    placeholder="Ex: exemple@mail.com" required="required"
                                                    t-att-value="request.params.get('email')"
                                                    pattern="[^@]+@[^@]+\.[a-zA-Z]{2,}"  
                                                    oninput="validateEmail(this)" 
                                                />
                                                <small class="form-text text-danger" id="email-error" style="display: none;">
                                                    L'email doit contenir un "@" et un domaine valide (ex: exemple@mail.com).
                                                </small>
                                            </div>
                                        </div>

                                        <!-- Modalité de paiement et Montnant demandé  -->
                                       <div class="row mb-4">
                                           <div class="col-md-6">
                                               <label class="form-label">
                                                   <i class="fa fa-user me-2"/>Modalité de remboursement 
                                               </label>
                                                <select id="modalite_remboursement" name="modalite_remboursement" class="form-control">
                                                    <option value="10">10 mois</option>
                                                    <option value="9">9 mois</option>
                                                    <option value="8">8 mois</option>
                                                    <option value="7">7 mois</option>
                                                    <option value="6">6 mois</option>
                                                    <option value="5">5 mois</option>
                                                    <option value="4">4 mois</option>
                                                    <option value="3">3 mois</option>
                                                </select>
                                           </div>
                                            <div class="col-md-6">
                                                <label class="form-label">
                                                    <i class="fa fa-money me-2"/>Montant Demandé (FCFA)
                                                </label>
                                                <div class="input-group">
                                                    <input type="text" 
                                                        id="amount_requested_display" 
                                                        class="form-control" 
                                                        required="1"
                                                        t-att-data-initial-value="request.params.get('amount_requested')"
                                                        placeholder="Montant de prêt limité à 9 999 999"
                                                        oninput="formatAmount(this)"/>
                                                    <input type="hidden" 
                                                        id="amount_requested" 
                                                        name="amount_requested" 
                                                        t-att-value="request.params.get('amount_requested')"/>
                                                        <span class="input-group-text">FCFA</span>
                                                </div>
                                            </div>
                                            <!-- LE SCRIPT DU SEPARATEUR DES MILLIERS   ET VALIDATION DE MAIL -->
                                                <script>
                                                    function formatAmount(input) {
                                                        // Supprimer tout sauf les chiffres
                                                        let value = input.value.replace(/\D/g, '');
                                                        
                                                        // Limiter à 7 chiffres
                                                        if (value.length > 7) {
                                                            value = value.slice(0, 7);
                                                        }
                                                        
                                                        // Convertir en nombre
                                                        const num = parseInt(value, 10);
                                                        
                                                        // Si c'est un nombre valide
                                                        if (!isNaN(num)) {
                                                            // Formater avec le séparateur de milliers
                                                            input.value = num.toLocaleString('fr-FR');
                                                            // Mettre à jour le champ caché avec la valeur non formatée
                                                            document.getElementById('amount_requested').value = num;
                                                        } else {
                                                            input.value = '';
                                                            document.getElementById('amount_requested').value = '';
                                                        }
                                                    }

                                                    // Formater la valeur initiale si elle existe
                                                    document.addEventListener('DOMContentLoaded', function() {
                                                        const display = document.getElementById('amount_requested_display');
                                                        const initialValue = display.dataset.initialValue;
                                                        if (initialValue) {
                                                            display.value = parseInt(initialValue).toLocaleString('fr-FR');
                                                            document.getElementById('amount_requested').value = initialValue;
                                                        }
                                                    });


                                                    // --------------------- VALIDATION DE MAIL -------------------------------
                                                    function validateEmail(input) {
                                                        const emailError = document.getElementById('email-error');
                                                        const emailPattern = /^[^@]+@[^@]+\.[a-zA-Z]{2,}$/;  // Expression régulière pour valider l'email

                                                        if (!emailPattern.test(input.value)) {
                                                            emailError.style.display = 'block';  // Afficher le message d'erreur
                                                            input.setCustomValidity('L\'email doit contenir un "@" et un domaine valide.');  // Validation HTML5
                                                            
                                                            // Empêcher de quitter le champ si l'email n'est pas valide
                                                            input.focus();
                                                            return false;
                                                        } else {
                                                            emailError.style.display = 'none';  // Masquer le message d'erreur
                                                            input.setCustomValidity('');  // Réinitialiser la validation HTML5
                                                            return true;
                                                        }
                                                    }

                                                    // Exemple d'utilisation avec l'événement blur
                                                    document.getElementById('email').addEventListener('blur', function(e) {
                                                        validateEmail(this);
                                                    });

                                                    // Vous pouvez également valider lors de la soumission du formulaire
                                                    document.getElementById('myForm').addEventListener('submit', function(e) {
                                                        const emailInput = document.getElementById('email');
                                                        if (!validateEmail(emailInput)) {
                                                            e.preventDefault(); // Empêcher la soumission si l'email n'est pas valide
                                                        }
                                                    });
                                                </script>
                                       </div>

                                       <!-- Bouton -->
                                       <div class="text-end">
                                           <button type="submit" class="btn btn-primary px-4" style="background-color:#0099CC">
                                               <i class="fa fa-paper-plane me-2"/>Soumettre la demande
                                           </button>
                                       </div>
                                   </form>
                               </div>
                           </div>
                       </div>
                   </div>
               </div>
           </div>
       </t>
   </template>
</odoo>